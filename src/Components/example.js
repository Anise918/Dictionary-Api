{isOpen && (
  <ul style={{position:'absolute', top:'2rem',}}
    className={`dropDown absolute left-0 mt-2 w-25 border rounded-lg shadow-lg z-10 ${
      theme === 'dark' ? 'darkTheme' : 'lightTheme'
    }`}>